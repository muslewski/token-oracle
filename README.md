# token-oracle

Observed-Rate Allowance & Cap-Limit Estimator — provider-agnostic token usage-cap forecaster.

## Overview

A Python package for forecasting token usage caps and rate limits across different LLM providers.
